<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

  <!-- ===================== Colors & Brushes ===================== -->
  <SolidColorBrush x:Key="PrimaryBrush" Color="#FF1565C0"/>
  <SolidColorBrush x:Key="PrimaryDarkBrush" Color="#FF0D47A1"/>
  <SolidColorBrush x:Key="PrimaryLightBrush" Color="#FF64B5F6"/>
  <SolidColorBrush x:Key="AccentBrush" Color="#FF1976D2"/>

  <SolidColorBrush x:Key="BackgroundBrush" Color="#FFF6F7FB"/>
  <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFF"/>
  <SolidColorBrush x:Key="SurfaceColor" Color="#FFFFFF"/>
  <SolidColorBrush x:Key="BorderBrush" Color="#22000000"/>
  <SolidColorBrush x:Key="ConsistentBorderBrush" Color="#22000000"/>

  <SolidColorBrush x:Key="TextPrimaryBrush" Color="#FF212121"/>
  <SolidColorBrush x:Key="TextSecondaryBrush" Color="#FF757575"/>
  <SolidColorBrush x:Key="TextOnPrimaryBrush" Color="#FFFFFF"/>
  <SolidColorBrush x:Key="SecondaryTextBrush" Color="#99000000"/>
  <SolidColorBrush x:Key="MutedBrush" Color="#7F000000"/>

  <SolidColorBrush x:Key="SuccessBrush" Color="#FF2E7D32"/>
  <SolidColorBrush x:Key="SuccessColor" Color="#FF2E7D32"/>
  <SolidColorBrush x:Key="DangerBrush" Color="#FFC62828"/>
  <SolidColorBrush x:Key="DangerColor" Color="#FFC62828"/>
  <SolidColorBrush x:Key="InfoBrush" Color="#FF1565C0"/>
  <SolidColorBrush x:Key="InfoColor" Color="#FF1565C0"/>
  <SolidColorBrush x:Key="WarningBrush" Color="#FFF57C00"/>
  <SolidColorBrush x:Key="WarningColor" Color="#FFF57C00"/>

  <!-- ===================== Typography ===================== -->
  <FontFamily x:Key="PrimaryFont">Segoe UI, Tahoma, Arial</FontFamily>
  <sys:Double x:Key="FontSizeDisplay">18</sys:Double>
  <sys:Double x:Key="FontSizeHeading">16</sys:Double>
  <sys:Double x:Key="FontSizeLarge">15</sys:Double>
  <sys:Double x:Key="FontSizeBody">13</sys:Double>
  <sys:Double x:Key="FontSizeCaption">11</sys:Double>

  <!-- ===================== Spacing ===================== -->
  <Thickness x:Key="SmallPadding">6,4,6,4</Thickness>
  <Thickness x:Key="MediumPadding">10,6,10,6</Thickness>
  <Thickness x:Key="LargePadding">14,10,14,10</Thickness>
  <Thickness x:Key="SmallMargin">6,0,0,0</Thickness>
  <Thickness x:Key="MediumMargin">10,0,0,0</Thickness>
  <Thickness x:Key="LargeMargin">14,0,0,0</Thickness>
  <Thickness x:Key="ExtraLargeMargin">24,0,0,0</Thickness>

  <!-- ===================== Icons (placeholders) ===================== -->
  <sys:String x:Key="MenuIcon">≡</sys:String>
  <sys:String x:Key="SuccessIcon">✓</sys:String>
  <sys:String x:Key="SettingsIcon">⚙</sys:String>
  <sys:String x:Key="RefreshIcon">↻</sys:String>
  <sys:String x:Key="HomeIcon">🏠</sys:String>

  <!-- ===================== Base Controls ===================== -->
  <Style x:Key="StandardTextBox" TargetType="TextBox">
    <Setter Property="Padding" Value="8,6"/>
    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
    <Setter Property="SnapsToDevicePixels" Value="True"/>
    <Setter Property="FontFamily" Value="{DynamicResource PrimaryFont}"/>
    <Setter Property="FontSize" Value="{DynamicResource FontSizeBody}"/>
  </Style>

  <Style x:Key="ModernTextBox" TargetType="TextBox" BasedOn="{StaticResource StandardTextBox}">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="TextBox">
          <Border Background="{TemplateBinding Background}" 
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="4"
                  Padding="{TemplateBinding Padding}">
            <ScrollViewer x:Name="PART_ContentHost" />
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- ===================== Buttons ===================== -->
  <Style x:Key="PrimaryButton" TargetType="Button">
    <Setter Property="Foreground" Value="{DynamicResource TextOnPrimaryBrush}"/>
    <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
    <Setter Property="Padding" Value="{DynamicResource MediumPadding}"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="FontFamily" Value="{DynamicResource PrimaryFont}"/>
    <Setter Property="FontSize" Value="{DynamicResource FontSizeBody}"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border Background="{TemplateBinding Background}" 
                  CornerRadius="4" 
                  Padding="{TemplateBinding Padding}">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter Property="Background" Value="{DynamicResource PrimaryDarkBrush}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
    <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
    <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
    <Setter Property="BorderThickness" Value="1"/>
  </Style>

  <Style x:Key="OutlinedButton" TargetType="Button">
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Padding" Value="{DynamicResource SmallPadding}"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="FontFamily" Value="{DynamicResource PrimaryFont}"/>
    <Setter Property="FontSize" Value="{DynamicResource FontSizeBody}"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border Background="{TemplateBinding Background}" 
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="4" 
                  Padding="{TemplateBinding Padding}">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
              <Setter Property="Foreground" Value="{DynamicResource TextOnPrimaryBrush}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="QuickActionButton" TargetType="Button" BasedOn="{StaticResource SecondaryButton}"/>
  <Style x:Key="ModernButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButton}"/>

  <!-- ===================== Containers ===================== -->
  <Style x:Key="Card" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="CornerRadius" Value="8"/>
    <Setter Property="Padding" Value="12"/>
    <Setter Property="SnapsToDevicePixels" Value="True"/>
    <Setter Property="Effect">
      <Setter.Value>
        <DropShadowEffect Color="Black" Opacity="0.1" BlurRadius="4" ShadowDepth="2" Direction="270"/>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="CardStyle" TargetType="Border" BasedOn="{StaticResource Card}"/>
  <Style x:Key="StandardCard" TargetType="Border" BasedOn="{StaticResource Card}"/>

  <Style x:Key="DashboardCard" TargetType="Border" BasedOn="{StaticResource Card}">
    <Setter Property="Width" Value="220"/>
    <Setter Property="Height" Value="140"/>
    <Setter Property="Margin" Value="5"/>
  </Style>

  <!-- ===================== Text Styles ===================== -->
  <Style x:Key="BodyTextBlock" TargetType="TextBlock">
    <Setter Property="FontSize" Value="{DynamicResource FontSizeBody}"/>
    <Setter Property="FontFamily" Value="{DynamicResource PrimaryFont}"/>
    <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
  </Style>

  <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
    <Setter Property="FontSize" Value="{DynamicResource FontSizeHeading}"/>
    <Setter Property="FontFamily" Value="{DynamicResource PrimaryFont}"/>
    <Setter Property="FontWeight" Value="SemiBold"/>
    <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
  </Style>

  <Style x:Key="ValueTextStyle" TargetType="TextBlock">
    <Setter Property="FontSize" Value="{DynamicResource FontSizeDisplay}"/>
    <Setter Property="FontFamily" Value="{DynamicResource PrimaryFont}"/>
    <Setter Property="FontWeight" Value="Bold"/>
    <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}"/>
  </Style>

  <!-- ===================== Layout Styles ===================== -->
  <Style x:Key="ModernMenu" TargetType="Menu">
    <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
    <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="Padding" Value="4"/>
    <Setter Property="FontFamily" Value="{DynamicResource PrimaryFont}"/>
  </Style>

  <Style x:Key="ModernStatusBar" TargetType="StatusBar">
    <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
    <Setter Property="BorderThickness" Value="1,1,1,0"/>
    <Setter Property="FontFamily" Value="{DynamicResource PrimaryFont}"/>
  </Style>

  <Style x:Key="SidebarLayout" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
    <Setter Property="BorderThickness" Value="0,0,1,0"/>
    <Setter Property="Padding" Value="0"/>
  </Style>

  <Style x:Key="TitleBarLayout" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
    <Setter Property="Padding" Value="10,6"/>
    <Setter Property="BorderBrush" Value="{DynamicResource PrimaryDarkBrush}"/>
    <Setter Property="BorderThickness" Value="0,0,0,1"/>
  </Style>

  <!-- ===================== Search Bar ===================== -->
  <Style x:Key="SearchBar" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="CornerRadius" Value="16"/>
    <Setter Property="Padding" Value="10,6"/>
  </Style>

  <!-- ===================== Product Button Style ===================== -->
  <Style x:Key="ProductButtonStyle" TargetType="Button">
    <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Padding" Value="15,8"/>
    <Setter Property="Margin" Value="5"/>
    <Setter Property="FontFamily" Value="{DynamicResource PrimaryFont}"/>
    <Setter Property="FontSize" Value="14"/>
    <Setter Property="FontWeight" Value="Medium"/>
    <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
    <Setter Property="FlowDirection" Value="RightToLeft"/>
    <Setter Property="HorizontalContentAlignment" Value="Center"/>
    <Setter Property="VerticalContentAlignment" Value="Center"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="8"
                  Padding="{TemplateBinding Padding}">
            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
              <Setter Property="Foreground" Value="White"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
              <Setter Property="Foreground" Value="White"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- ===================== Expander/Sidebar Elements ===================== -->
  <Style x:Key="ModernExpander" TargetType="Expander">
    <Setter Property="Margin" Value="0,8,0,0"/>
    <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
    <Setter Property="FontFamily" Value="{DynamicResource PrimaryFont}"/>
  </Style>

  <Style x:Key="SidebarButton" TargetType="Button" BasedOn="{StaticResource OutlinedButton}">
    <Setter Property="HorizontalAlignment" Value="Stretch"/>
    <Setter Property="Margin" Value="0,4,0,0"/>
  </Style>

  <!-- ===================== Notifications ===================== -->
  <Style x:Key="NotificationItemStyle" TargetType="Border" BasedOn="{StaticResource Card}">
    <Setter Property="Background" Value="#FFFDFDFD"/>
    <Setter Property="Width" Value="250"/>
  </Style>

  <!-- ===================== Summary Card Style ===================== -->
  <Style x:Key="SummaryCardStyle" TargetType="Border" BasedOn="{StaticResource Card}">
    <Setter Property="MinHeight" Value="100"/>
    <Setter Property="Margin" Value="10"/>
  </Style>

  <!-- ===================== Window Base Style ===================== -->
  <Style x:Key="StandardWindow" TargetType="Window">
    <Setter Property="FontFamily" Value="{DynamicResource PrimaryFont}"/>
    <Setter Property="FontSize" Value="{DynamicResource FontSizeBody}"/>
    <Setter Property="Background" Value="{DynamicResource BackgroundBrush}"/>
    <Setter Property="UseLayoutRounding" Value="True"/>
    <Setter Property="SnapsToDevicePixels" Value="True"/>
    <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
    <Setter Property="WindowStartupLocation" Value="CenterScreen"/>
    <Setter Property="FlowDirection" Value="RightToLeft"/>
  </Style>

  <!-- ===================== Default Window Style ===================== -->
  <Style TargetType="Window" BasedOn="{StaticResource StandardWindow}"/>

</ResourceDictionary>