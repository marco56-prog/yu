<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

  <!-- ===== Color values ===== -->
  <Color x:Key="PrimaryColorValue">#2563EB</Color>
  <Color x:Key="BackgroundColorValue">#F5F7FB</Color>
  <Color x:Key="SurfaceColorValue">#FFFFFF</Color>
  <Color x:Key="BorderColorValue">#E5E7EB</Color>

  <Color x:Key="TextPrimaryColorValue">#111827</Color>
  <Color x:Key="TextSecondaryColorValue">#6B7280</Color>
  <Color x:Key="MutedColorValue">#9CA3AF</Color>

  <Color x:Key="SuccessColorValue">#16A34A</Color>
  <Color x:Key="DangerColorValue">#DC2626</Color>
  <Color x:Key="WarningColorValue">#D97706</Color>
  <Color x:Key="InfoColorValue">#0EA5E9</Color>

  <!-- Text on colors (للنص على الخلفيات الملونة) -->
  <Color x:Key="TextOnPrimaryColorValue">#FFFFFF</Color>
  <Color x:Key="TextOnSurfaceColorValue">#111827</Color>
  <Color x:Key="TextOnSuccessColorValue">#FFFFFF</Color>
  <Color x:Key="TextOnDangerColorValue">#FFFFFF</Color>
  <Color x:Key="TextOnWarningColorValue">#FFFFFF</Color>
  <Color x:Key="TextOnInfoColorValue">#FFFFFF</Color>

  <!-- ===== Brushes (استعمل دي في XAML) ===== -->
  <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColorValue}"/>
  <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColorValue}"/>
  <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColorValue}"/>
  <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColorValue}"/>

  <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColorValue}"/>
  <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColorValue}"/>
  <SolidColorBrush x:Key="MutedBrush" Color="{StaticResource MutedColorValue}"/>

  <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColorValue}"/>
  <SolidColorBrush x:Key="DangerBrush" Color="{StaticResource DangerColorValue}"/>
  <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColorValue}"/>
  <SolidColorBrush x:Key="InfoBrush" Color="{StaticResource InfoColorValue}"/>

  <!-- Text on color brushes -->
  <SolidColorBrush x:Key="TextOnPrimaryColor" Color="{StaticResource TextOnPrimaryColorValue}"/>
  <SolidColorBrush x:Key="TextOnSurfaceColor" Color="{StaticResource TextOnSurfaceColorValue}"/>
  <SolidColorBrush x:Key="TextOnSuccessColor" Color="{StaticResource TextOnSuccessColorValue}"/>
  <SolidColorBrush x:Key="TextOnDangerColor" Color="{StaticResource TextOnDangerColorValue}"/>
  <SolidColorBrush x:Key="TextOnWarningColor" Color="{StaticResource TextOnWarningColorValue}"/>
  <SolidColorBrush x:Key="TextOnInfoColor" Color="{StaticResource TextOnInfoColorValue}"/>

  <!-- Accent (لو عايزه) -->
  <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource PrimaryColorValue}"/>
  
  <!-- ألوان إضافية مطلوبة للفواتير -->
  <SolidColorBrush x:Key="SurfaceAltBrush" Color="#FAFBFC"/>
  <SolidColorBrush x:Key="BorderBoldBrush" Color="#D1D5DB"/>
  <SolidColorBrush x:Key="AccentSoftBrush" Color="#EEF2FF"/>
  <SolidColorBrush x:Key="AccentHoverBrush" Color="#1D4ED8"/>
  <SolidColorBrush x:Key="AccentPressedBrush" Color="#1E40AF"/>
  <SolidColorBrush x:Key="DisabledTextBrush" Color="#9CA3AF"/>
  
  <!-- موارد للتوافق مع النوافذ -->
  <SolidColorBrush x:Key="SecondaryColor" Color="{StaticResource SurfaceColorValue}"/>
  <SolidColorBrush x:Key="CardBackground" Color="{StaticResource SurfaceColorValue}"/>
  <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextPrimaryColorValue}"/>

  <!-- Typography -->
  <FontFamily x:Key="AppFontFamily">Segoe UI, Tahoma, Arial</FontFamily>
  <sys:Double x:Key="FontSizeXS">11</sys:Double>
  <sys:Double x:Key="FontSizeSmall">12</sys:Double>
  <sys:Double x:Key="FontSizeNormal">13</sys:Double>
  <sys:Double x:Key="FontSizeLarge">14</sys:Double>
  <sys:Double x:Key="FontSizeXL">16</sys:Double>

  <!-- Radii -->
  <CornerRadius x:Key="RadiusS">4</CornerRadius>
  <CornerRadius x:Key="RadiusM">8</CornerRadius>
  <CornerRadius x:Key="RadiusL">12</CornerRadius>

  <!-- Card -->
  <Style x:Key="Card" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="CornerRadius" Value="{DynamicResource RadiusM}"/>
    <Setter Property="Padding" Value="10"/>
    <Setter Property="SnapsToDevicePixels" Value="True"/>
    <Setter Property="Effect">
      <Setter.Value>
        <DropShadowEffect BlurRadius="4" Direction="270" ShadowDepth="1" Color="Black" Opacity="0.05"/>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- CardStyle (مطلوب للنوافذ) -->
  <Style x:Key="CardStyle" TargetType="Border" BasedOn="{StaticResource Card}"/>

  <!-- Buttons -->
  <Style x:Key="PrimaryButtonStyle" TargetType="Button">
    <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}"/>
    <Setter Property="FontSize" Value="{DynamicResource FontSizeSmall}"/>
    <Setter Property="Foreground" Value="White"/>
    <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
    <Setter Property="Padding" Value="10,6"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border Background="{TemplateBinding Background}" CornerRadius="{DynamicResource RadiusS}">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True"><Setter Property="Opacity" Value="0.92"/></Trigger>
            <Trigger Property="IsPressed" Value="True"><Setter Property="Opacity" Value="0.85"/></Trigger>
            <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.6"/></Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
    <Setter Property="Background" Value="#EEF2FF"/>
    <Setter Property="Foreground" Value="#1E40AF"/>
    <Setter Property="BorderBrush" Value="#C7D2FE"/>
    <Setter Property="BorderThickness" Value="1"/>
  </Style>
  <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}"/>
  
  <!-- أسماء بديلة مطلوبة -->
  <Style x:Key="ButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}"/>
  <Style x:Key="ModernButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}"/>

  <!-- TextBox -->
  <Style x:Key="ModernTextBox" TargetType="TextBox">
    <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}"/>
    <Setter Property="FontSize" Value="{DynamicResource FontSizeSmall}"/>
    <Setter Property="Padding" Value="8,5"/>
    <Setter Property="Background" Value="White"/>
    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="VerticalContentAlignment" Value="Center"/>
  </Style>

  <!-- Window defaults -->
  <Style x:Key="ModernWindow" TargetType="Window">
    <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}"/>
    <Setter Property="FontSize" Value="{DynamicResource FontSizeNormal}"/>
    <Setter Property="Background" Value="{DynamicResource BackgroundBrush}"/>
    <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
  </Style>
  <Style TargetType="Window" BasedOn="{StaticResource ModernWindow}"/>
  <Style TargetType="UserControl">
    <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}"/>
    <Setter Property="FontSize" Value="{DynamicResource FontSizeNormal}"/>
    <Setter Property="Background" Value="{DynamicResource BackgroundBrush}"/>
    <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
  </Style>
  <Style TargetType="Page">
    <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}"/>
    <Setter Property="FontSize" Value="{DynamicResource FontSizeNormal}"/>
    <Setter Property="Background" Value="{DynamicResource BackgroundBrush}"/>
    <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
  </Style>

</ResourceDictionary>