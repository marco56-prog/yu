<Window x:Class="AccountingSystem.WPF.Views.CashTransactionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ط­ط±ظƒط© ط®ط²ظ†ط©" Height="460" Width="560"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        FlowDirection="RightToLeft"
        Background="{StaticResource MasterBackgroundBrush}">

    <Window.Resources>
        <!-- ط²ط± ط­ط¯ظٹط« -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="Margin" Value="6,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.75"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ط¨ط·ط§ظ‚ط© -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource MasterSurfaceBrush}"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="18"/>
            <Setter Property="Margin" Value="0,0,0,14"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="{StaticResource ShadowColor}" Opacity="0.06" BlurRadius="10" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- ط§ظ„ظ‡ظٹط¯ط± -->
            <RowDefinition Height="*"/>     <!-- ط§ظ„ظ…ط­طھظˆظ‰ -->
            <RowDefinition Height="Auto"/>  <!-- ط§ظ„ط£ط²ط±ط§ط± -->
        </Grid.RowDefinitions>

        <!-- ط§ظ„ظ‡ظٹط¯ط± -->
        <Border Grid.Row="0" Background="{StaticResource MasterSurfaceBrush}" Padding="16" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal">
                    <Border Background="{StaticResource MasterPrimaryBrush}" Width="40" Height="40" CornerRadius="20" Margin="0,0,10,0">
                        <TextBlock Text="ًں’µ" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>
                    <StackPanel>
                        <TextBlock Text="طھط³ط¬ظٹظ„ ط­ط±ظƒط© ط®ط²ظ†ط©" FontSize="{StaticResource FontSizeLarge}" FontWeight="Bold" Foreground="{StaticResource TextPrimaryColor}"/>
                        <TextBlock Text="ط£ط¯ط®ظ„ طھظپط§طµظٹظ„ ط§ظ„ط­ط±ظƒط© ط§ظ„ظ…ط§ظ„ظٹط©" FontSize="{StaticResource FontSizeSmall}" Foreground="{StaticResource TextSecondaryColor}"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ط§ظ„ظ…ط­طھظˆظ‰ -->
        <StackPanel Grid.Row="1" Margin="18">
            <!-- ظ…ط¹ظ„ظˆظ…ط§طھ ط¹ط§ظ…ط© -->
            <Border Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- ظ†ظˆط¹ ط§ظ„ط­ط±ظƒط© -->
                    <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,10,8">
                        <TextBlock Text="ظ†ظˆط¹ ط§ظ„ط­ط±ظƒط©" FontWeight="Bold" Margin="0,0,0,4"/>
                        <TextBlock Name="txtTransactionType" FontSize="{StaticResource FontSizeNormal}" FontWeight="Bold"/>
                    </StackPanel>

                    <!-- ط§ظ„ط®ط²ظ†ط© -->
                    <StackPanel Grid.Row="0" Grid.Column="1" Margin="10,0,0,8">
                        <TextBlock Text="ط§ظ„ط®ط²ظ†ط©" FontWeight="Bold" Margin="0,0,0,4"/>
                        <TextBlock Name="txtCashBox" FontSize="{StaticResource FontSizeNormal}"/>
                    </StackPanel>

                    <!-- ط§ظ„ظ…ط¨ظ„ط؛ -->
                    <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Margin="0,8,0,0">
                        <TextBlock Text="ط§ظ„ظ…ط¨ظ„ط؛ *" FontWeight="Bold" Margin="0,0,0,4"/>
                        <TextBox Name="txtAmount" Height="40" FontSize="{StaticResource FontSizeMedium}"
                                 Padding="10" BorderBrush="{StaticResource BorderColor}" BorderThickness="2"
                                 PreviewTextInput="txtAmount_PreviewTextInput"
                                 DataObject.Pasting="txtAmount_Pasting"/>
                        <TextBlock Text="ط§ط³طھط®ط¯ظ… ظپط§طµظ„ط©/ظ†ظ‚ط·ط© ظ„ظ„ظپط§طµظ„ ط§ظ„ط¹ط´ط±ظٹ ط­ط³ط¨ ط¥ط¹ط¯ط§ط¯ط§طھ ط¬ظ‡ط§ط²ظƒ"
                                   Foreground="{StaticResource TextMutedColor}" FontSize="{StaticResource FontSizeXSmall}" Margin="4,4,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- طھظپط§طµظٹظ„ -->
            <Border Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- ط§ظ„ظˆطµظپ -->
                    <StackPanel Grid.Row="0" Margin="0,0,0,10">
                        <TextBlock Text="ظˆطµظپ ط§ظ„ط­ط±ظƒط© *" FontWeight="Bold" Margin="0,0,0,4"/>
                        <TextBox Name="txtDescription" Height="36" FontSize="{StaticResource FontSizeNormal}" Padding="10" BorderBrush="{StaticResource BorderColor}" BorderThickness="2"/>
                    </StackPanel>

                    <!-- ط§ط³ظ… ط§ظ„ط¹ظ…ظٹظ„/ط§ظ„ظ…ظˆط±ط¯ -->
                    <StackPanel Grid.Row="1" Margin="0,0,0,10">
                        <TextBlock Text="ط§ط³ظ… ط§ظ„ط¹ظ…ظٹظ„/ط§ظ„ظ…ظˆط±ط¯ (ط§ط®طھظٹط§ط±ظٹ)" FontWeight="Bold" Margin="0,0,0,4"/>
                        <TextBox Name="txtCustomerSupplier" Height="36" FontSize="{StaticResource FontSizeNormal}" Padding="10" BorderBrush="{StaticResource BorderColor}" BorderThickness="2"/>
                    </StackPanel>

                    <!-- ظ…ظ„ط§ط­ط¸ط§طھ -->
                    <StackPanel Grid.Row="2">
                        <TextBlock Text="ظ…ظ„ط§ط­ط¸ط§طھ" FontWeight="Bold" Margin="0,0,0,4"/>
                        <TextBox Name="txtNotes" Height="80" FontSize="{StaticResource FontSizeNormal}" Padding="10"
                                 BorderBrush="{StaticResource BorderColor}" BorderThickness="2"
                                 TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
                    </StackPanel>
                </Grid>
            </Border>
        </StackPanel>

        <!-- ط§ظ„ط£ط²ط±ط§ط± -->
        <Border Grid.Row="2" Background="{StaticResource MasterSurfaceBrush}" Padding="12" BorderBrush="{StaticResource BorderColor}" BorderThickness="1,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Name="btnSave" Content="ط­ظپط¸" Width="130" Height="42"
                        Style="{StaticResource SuccessButton}"
                        Click="btnSave_Click" IsDefault="True"/>
                <Button Name="btnCancel" Content="ط¥ظ„ط؛ط§ط،" Width="110" Height="42"
                        Style="{StaticResource SecondaryButton}"
                        Click="btnCancel_Click" IsCancel="True"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
