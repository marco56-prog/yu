<Window x:Class="AccountingSystem.WPF.Views.CashTransferDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="طھط­ظˆظٹظ„ ط¨ظٹظ† ط§ظ„ط®ط²ط§ط¦ظ†"
        Height="380" Width="560"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        FlowDirection="RightToLeft"
        Background="{StaticResource MasterBackgroundBrush}"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="Margin" Value="6,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.75"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource MasterSurfaceBrush}"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="18"/>
            <Setter Property="Margin" Value="0,0,0,14"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="{StaticResource ShadowColor}" Opacity="0.06" BlurRadius="10" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource MasterSurfaceBrush}" Padding="14" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,1" CornerRadius="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal">
                    <Border Background="{StaticResource MasterPrimaryBrush}" Width="40" Height="40" CornerRadius="20" Margin="0,0,10,0">
                        <TextBlock Text="ًں’±" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>
                    <StackPanel>
                        <TextBlock Text="طھط­ظˆظٹظ„ ط¨ظٹظ† ط§ظ„ط®ط²ط§ط¦ظ†" FontSize="{StaticResource FontSizeLarge}" FontWeight="Bold" Foreground="{StaticResource TextPrimaryColor}"/>
                        <TextBlock Text="ط§ظ†ظ‚ظ„ ظ…ط¨ظ„ط؛ظ‹ط§ ظ…ظ† ط®ط²ظ†ط© ط¥ظ„ظ‰ ط£ط®ط±ظ‰" FontSize="{StaticResource FontSizeSmall}" Foreground="{StaticResource TextSecondaryColor}"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content -->
        <StackPanel Grid.Row="1">
            <Border Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Source -->
                    <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,10,12">
                        <TextBlock Text="ظ…ظ† ط§ظ„ط®ط²ظ†ط©" FontWeight="Bold"/>
                        <TextBlock Name="txtSourceCashBox" FontSize="{StaticResource FontSizeNormal}" Foreground="{StaticResource PrimaryColor}" />
                    </StackPanel>

                    <!-- Target -->
                    <StackPanel Grid.Row="0" Grid.Column="1" Margin="10,0,0,12">
                        <TextBlock Text="ط¥ظ„ظ‰ ط§ظ„ط®ط²ظ†ط© *" FontWeight="Bold"/>
                        <ComboBox Name="cmbTargetCashBox" Height="34" FontSize="{StaticResource FontSizeNormal}"
                                  DisplayMemberPath="CashBoxName"/>
                    </StackPanel>

                    <!-- Amount -->
                    <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Margin="0,0,0,12">
                        <TextBlock Text="ط§ظ„ظ…ط¨ظ„ط؛ *" FontWeight="Bold"/>
                        <TextBox Name="txtAmount" Height="40" FontSize="{StaticResource FontSizeMedium}" Padding="10"
                                 HorizontalContentAlignment="Center"
                                 BorderBrush="{StaticResource BorderColor}" BorderThickness="2"
                                 PreviewTextInput="txtAmount_PreviewTextInput"
                                 DataObject.Pasting="txtAmount_Pasting"/>
                        <TextBlock Text="ط§ط³طھط®ط¯ظ… ظپط§طµظ„ ط¹ط´ط±ظٹ ط­ط³ط¨ ط¥ط¹ط¯ط§ط¯ط§طھ ط¬ظ‡ط§ط²ظƒ" FontSize="{StaticResource FontSizeXSmall}" Foreground="{StaticResource TextMutedColor}" Margin="4,4,0,0"/>
                    </StackPanel>

                    <!-- Description -->
                    <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Margin="0,0,0,0">
                        <TextBlock Text="ظˆطµظپ ط§ظ„طھط­ظˆظٹظ„ *" FontWeight="Bold"/>
                        <TextBox Name="txtDescription" Height="34" FontSize="{StaticResource FontSizeNormal}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="ظ…ظ„ط§ط­ط¸ط§طھ" FontWeight="Bold"/>
                    <TextBox Name="txtNotes" Height="70" FontSize="{StaticResource FontSizeSmall}"
                             TextWrapping="Wrap" AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Footer Buttons -->
        <Border Grid.Row="2" Background="{StaticResource MasterSurfaceBrush}" Padding="12" BorderBrush="{StaticResource BorderColor}" BorderThickness="1,1,0,0" CornerRadius="8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Name="btnTransfer" Content="طھط­ظˆظٹظ„" Width="130" Height="42"
                        Style="{StaticResource WarningButton}"
                        Click="btnTransfer_Click" IsDefault="True"/>
                <Button Name="btnCancel" Content="ط¥ظ„ط؛ط§ط،" Width="110" Height="42"
                        Style="{StaticResource SecondaryButton}"
                        Click="btnCancel_Click" IsCancel="True"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
