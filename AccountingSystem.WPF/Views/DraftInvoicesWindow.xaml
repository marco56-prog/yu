<Window x:Class="AccountingSystem.WPF.Views.DraftInvoicesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="النظام المحاسبي - مسودات الفواتير"
        Height="700" Width="1200"
        Style="{StaticResource StandardWindow}"
        WindowState="Maximized">

    <!-- Window resources removed to use centralized Resources/UnifiedStyles.xaml -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text=" �������� ��������"
                           FontSize="24" 
                           FontWeight="Bold" 
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}">
            <DataGrid AutoGenerateColumns="False"
                      IsReadOnly="True"
                      GridLinesVisibility="None"
                      HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="��� �������" Width="120"/>
                    <DataGridTextColumn Header="��� ��������" Width="120"/>
                    <DataGridTextColumn Header="������/������" Width="200"/>
                    <DataGridTextColumn Header="������ ��������" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Grid.Row="2" 
                Background="#F1F5F9" 
                Padding="15,12"
                Margin="0,20,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Content=" �����"
                        Style="{StaticResource ModernButtonStyle}" 
                        Background="#6B7280"
                        Click="Close_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
