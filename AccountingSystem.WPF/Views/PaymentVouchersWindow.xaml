<Window x:Class="AccountingSystem.WPF.Views.PaymentVouchersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ط³ظ†ط¯ط§طھ ط§ظ„طµط±ظپ"
        Height="600" Width="1000" 
        WindowStartupLocation="CenterScreen"
        FlowDirection="RightToLeft">
    
    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource DangerBrush}" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Path Fill="White" Width="24" Height="24" Margin="0,0,10,0" 
                          Data="M12,16A3,3 0 0,1 9,13C9,11.88 9.61,10.9 10.5,10.39L20.21,4.77L21.68,6.24L6.24,21.68L4.77,20.21L10.39,14.5C9.9,13.61 9,12.88 9,11A3,3 0 0,1 12,8A3,3 0 0,1 15,11A3,3 0 0,1 12,16M12,10A1,1 0 0,0 11,11A1,1 0 0,0 12,12A1,1 0 0,0 13,11A1,1 0 0,0 12,10Z"/>
                    <TextBlock Text="ط³ظ†ط¯ط§طھ ط§ظ„طµط±ظپ" FontSize="{StaticResource FontSizeTitle}" FontWeight="Bold"
                               Foreground="{StaticResource TextOnPrimaryColor}" VerticalAlignment="Center"/>
                </StackPanel>
                
                <Button Grid.Column="1" Name="btnNewPayment"
                        Click="btnNewPayment_Click"
                        Background="{StaticResource SuccessBrush}" Foreground="{StaticResource TextOnPrimaryColor}"
                        Padding="15,8" BorderThickness="0" 
                        FontSize="{StaticResource FontSizeNormal}" FontWeight="Bold"
                        Style="{DynamicResource ModernButtonStyle}">
                    <StackPanel Orientation="Horizontal">
                        <Path Fill="White" Width="16" Height="16" Margin="0,0,5,0"
                              Data="M19,13H13V19H11V13H5V11H11V5H13V11H19V13Z"/>
                        <TextBlock Text="ط³ظ†ط¯ طµط±ظپ ط¬ط¯ظٹط¯"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Search and Filters -->
            <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" CornerRadius="8" 
                    BorderBrush="{StaticResource BorderColor}" BorderThickness="1" Padding="15" Margin="0,0,0,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Search Box -->
                    <TextBox Grid.Column="0" Name="txtSearch" 
                             Text="ط§ظ„ط¨ط­ط« ط¨ط±ظ‚ظ… ط§ظ„ط³ظ†ط¯ ط£ظˆ ط§ظ„ظ…ظˆط±ط¯..."
                             Foreground="{StaticResource TextMutedColor}" Padding="10" 
                             FontSize="{StaticResource FontSizeNormal}" Height="36"
                             BorderBrush="{StaticResource BorderColor}" BorderThickness="1"/>

                    <!-- Date From -->
                    <DatePicker Grid.Column="1" Name="dpDateFrom" Margin="10,0"
                                Width="120" Height="36"/>

                    <!-- Date To -->
                    <DatePicker Grid.Column="2" Name="dpDateTo" Margin="10,0"
                                Width="120" Height="36"/>

                    <!-- Search Button -->
                    <Button Grid.Column="3" Name="btnSearch"
                            Click="btnSearch_Click"
                            Background="{StaticResource PrimaryBrush}" Foreground="{StaticResource TextOnPrimaryColor}"
                            Padding="15,8" BorderThickness="0"
                            FontSize="{StaticResource FontSizeNormal}" Height="36">
                        <StackPanel Orientation="Horizontal">
                            <Path Fill="White" Width="16" Height="16" Margin="0,0,5,0"
                                  Data="M9.5,3A6.5,6.5 0 0,1 16,9.5C16,11.11 15.41,12.59 14.44,13.73L14.71,14H15.5L20.5,19L19,20.5L14,15.5V14.71L13.73,14.44C12.59,15.41 11.11,16 9.5,16A6.5,6.5 0 0,1 3,9.5A6.5,6.5 0 0,1 9.5,3M9.5,5C7,5 5,7 5,9.5C5,12 7,14 9.5,14C12,14 14,12 14,9.5C14,7 12,5 9.5,5Z"/>
                            <TextBlock Text="ط¨ط­ط«"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>

            <!-- Payments List -->
            <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" CornerRadius="8" 
                    BorderBrush="{StaticResource BorderColor}" BorderThickness="1" Padding="0">
                <Grid>
                    <!-- Sample Data Grid -->
                    <DataGrid Name="dgPayments" AutoGenerateColumns="False"
                              HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                              Background="{StaticResource SurfaceBrush}" RowBackground="{StaticResource SurfaceBrush}" 
                              AlternatingRowBackground="{StaticResource BackgroundBrush}"
                              BorderThickness="0" CanUserAddRows="False"
                              FontSize="{StaticResource FontSizeSmall}">
                        
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ط±ظ‚ظ… ط§ظ„ط³ظ†ط¯" Width="100" Binding="{Binding PaymentNumber}"/>
                            <DataGridTextColumn Header="ط§ظ„طھط§ط±ظٹط®" Width="100" Binding="{Binding Date}"/>
                            <DataGridTextColumn Header="ط§ظ„ظ…ط³طھظپظٹط¯" Width="200" Binding="{Binding PayeeName}"/>
                            <DataGridTextColumn Header="ط§ظ„ظ…ط¨ظ„ط؛" Width="120" Binding="{Binding Amount}"/>
                            <DataGridTextColumn Header="ط·ط±ظٹظ‚ط© ط§ظ„ط¯ظپط¹" Width="120" Binding="{Binding PaymentMethod}"/>
                            <DataGridTextColumn Header="ط§ظ„ط¨ظٹط§ظ†" Width="*" Binding="{Binding Description}"/>
                            <DataGridTextColumn Header="ط§ظ„ظ…ط­ط§ط³ط¨" Width="120" Binding="{Binding Accountant}"/>
                            <DataGridTemplateColumn Header="ط§ظ„ط¥ط¬ط±ط§ط،ط§طھ" Width="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Button Content="ط·ط¨ط§ط¹ط©" Background="{StaticResource InfoBrush}" Foreground="{StaticResource TextOnPrimaryColor}"
                                                    Padding="8,4" Margin="2" BorderThickness="0" FontSize="11"/>
                                            <Button Content="طھط¹ط¯ظٹظ„" Background="{StaticResource WarningBrush}" Foreground="{StaticResource TextPrimaryColor}"
                                                    Padding="8,4" Margin="2" BorderThickness="0" FontSize="11"/>
                                            <Button Content="ط­ط°ظپ" Background="{StaticResource DangerBrush}" Foreground="{StaticResource TextOnPrimaryColor}"
                                                    Padding="8,4" Margin="2" BorderThickness="0" FontSize="11"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- No Data Message -->
                    <StackPanel Name="pnlNoData" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Path Fill="{StaticResource BorderColor}" Width="64" Height="64" Margin="0,0,0,20"
                              Data="M12,16A3,3 0 0,1 9,13C9,11.88 9.61,10.9 10.5,10.39L20.21,4.77L21.68,6.24L6.24,21.68L4.77,20.21L10.39,14.5C9.9,13.61 9,12.88 9,11A3,3 0 0,1 12,8A3,3 0 0,1 15,11A3,3 0 0,1 12,16M12,10A1,1 0 0,0 11,11A1,1 0 0,0 12,12A1,1 0 0,0 13,11A1,1 0 0,0 12,10Z"/>
                        <TextBlock Text="ظ„ط§ طھظˆط¬ط¯ ط³ظ†ط¯ط§طھ طµط±ظپ" FontSize="{StaticResource FontSizeLarge}"
                                   Foreground="{StaticResource TextMutedColor}" FontWeight="SemiBold"/>
                        <TextBlock Text="ظ‚ظ… ط¨ط¥ط¶ط§ظپط© ط³ظ†ط¯ طµط±ظپ ط¬ط¯ظٹط¯ ظ„ظ„ط¨ط¯ط،" FontSize="{StaticResource FontSizeNormal}"
                                   Foreground="{StaticResource TextMutedColor}" Margin="0,5,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>

