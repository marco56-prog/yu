<Window x:Class="AccountingSystem.WPF.Views.ProductEntryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Product Details" Height="400" Width="450"
        WindowStartupLocation="CenterOwner"
        Style="{StaticResource ModernWindowStyle}">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Product Code:" Style="{StaticResource ModernLabel}"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding ProductCode, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" />

        <Label Grid.Row="1" Grid.Column="0" Content="Product Name:" Style="{StaticResource ModernLabel}"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding ProductName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" />

        <Label Grid.Row="2" Grid.Column="0" Content="Description:" Style="{StaticResource ModernLabel}"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" />

        <Label Grid.Row="3" Grid.Column="0" Content="Price:" Style="{StaticResource ModernLabel}"/>
        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding SalePrice, StringFormat='N2', UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" />

        <Label Grid.Row="4" Grid.Column="0" Content="Cost:" Style="{StaticResource ModernLabel}"/>
        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding PurchasePrice, StringFormat='N2', UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" />

        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnOK" Content="OK" Click="btnOK_Click" Style="{StaticResource PrimaryButton}" Margin="0,0,10,0"/>
            <Button x:Name="btnCancel" Content="Cancel" IsCancel="True" Style="{StaticResource ModernButton}"/>
        </StackPanel>
    </Grid>
</Window>