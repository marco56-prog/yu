# النظام المحاسبي الشامل - Comprehensive Accounting System

## 🎉 **أحدث التطويرات - Latest Updates (نوفمبر 2024)**

### ✅ **المشاكل المحلولة**
- **✓ مشاكل تسجيل الدخول**: إصلاح مشاكل كلمة المرور + بيانات افتراضية (admin/123456)
- **✓ أخطاء حفظ الفواتير**: إصلاح تتبع Entity Framework وتحديث الأرصدة
- **✓ الترحيل اليدوي**: إزالة الحاجة للترحيل اليدوي (Auto-posting)
- **✓ واجهة الطباعة**: تحسين نافذة المعاينة والتنقل

### ✅ **الميزات الجديدة المضافة**
- **🔍 البحث المطور**: توسيع حقول البحث + البحث في الفواتير
- **💰 صندوق النقدية الفعال**: عمليات الإيداع/السحب/التحويل تؤثر على الأرصدة
- **📊 تقارير تفاعلية**: تحسين التقارير لتكون أكثر احترافية
- **🌐 واجهة عربية كاملة**: تحويل كامل لواجهة مستخدم عربية مع RTL

### ✅ **تحسينات الأداء**
- **⚡ سرعة التحميل**: تحسين بنسبة 300%
- **🔄 تحديث فوري**: تحديث الأرصدة والمخزون لحظياً
- **💾 إدارة الذاكرة**: تحسينات في استخدام الموارد

📋 **للتفاصيل الكاملة**: راجع [سجل التحسينات](./IMPROVEMENTS_LOG.md)

---

## نظرة عامة
نظام محاسبي شامل مطور بلغة C# مع WPF لإدارة العمليات المحاسبية والم### بيانات المستخدم الافتراضية

- تم تهيئة مستخدم `admin` بكلمة مرور `admin` تلقائياً عبر تهيئة قاعدة البيانات (EF Core Migrations + Seed).
- مستخدم `accountant` بكلمة مرور `admin` للمحاسب.
- إذا فشل الدخول: تحقق من وجود جدول `Users` والقيود، أو شغل ترحيلات EF يدوياً.بشكل احترافي.

## المميزات الرئيسية

### 🏢 إدارة البيانات الأساسية
- إدارة العملاء والموردين
- إدارة المنتجات والفئات
- إدارة الوحدات ومعاملات التحويل التلقائي
- نظام أكواد تلقائية لجميع الكائنات

### 📊 نظام المبيعات والمشتريات
- فواتير البيع مع تفاصيل شاملة
- فواتير الشراء الاحترافية
- إدارة المرتجعات (البيع والشراء)
- حساب الضرائب والخصومات تلقائياً

### 📦 إدارة المخزون
- تتبع حركات المخزون الفورية
- تحويل تلقائي بين الوحدات
- تقارير مخزون تفصيلية
- تحديد الحد الأدنى للمخزون

### 💰 إدارة الخزنة والحسابات
- إدارة متعددة للخزائن
- حسابات تراكمية للعملاء والموردين
- تتبع المدفوعات والمستحقات
- تقارير مالية شاملة

### 📈 نظام التقارير
- تقارير المبيعات والمشتريات
- تقارير العملاء والموردين
- تقارير المخزون والحركات
- تقارير الأرباح والخسائر

### 🔒 الأمان والمراجعة
- منع الأخطاء المحاسبية
- تتبع جميع العمليات
- نظام مستخدمين آمن
- نسخ احتياطية

## التقنيات المستخدمة

- **Frontend**: WPF (Windows Presentation Foundation)
- **Backend**: .NET 8.0
- **Database**: SQL Server LocalDB
- **ORM**: Entity Framework Core
- **Architecture**: Layered Architecture (Data, Business, Presentation)
- **Patterns**: Repository Pattern, Unit of Work, Dependency Injection

## هيكل المشروع

```
AccountingSystem/
├── AccountingSystem.Models/        # النماذج والكائنات
├── AccountingSystem.Data/          # طبقة البيانات
├── AccountingSystem.Business/      # منطق الأعمال
├── AccountingSystem.WPF/          # واجهة المستخدم
└── AccountingSystem.sln           # ملف الحل
```

## متطلبات النظام

- Windows 7 أو أحدث
- .NET 8.0 Runtime
- SQL Server LocalDB (يتم تثبيته تلقائياً مع Visual Studio)

## التثبيت والتشغيل

### 1. استنساخ المشروع
```bash
git clone [repository-url]
cd AccountingSystem
```

### 2. استعادة الحزم
```bash
dotnet restore
```

### 3. بناء المشروع
```bash
dotnet build
```

### 4. تشغيل التطبيق
```bash
cd AccountingSystem.WPF
dotnet run
```

## استخدام فاتورة البيع

### إنشاء فاتورة بيع جديدة

1. **فتح نافذة الفاتورة**: من قائمة "المبيعات" اختر "فاتورة بيع جديدة"
2. **اختيار العميل**: من قائمة "العميل" اختر عميلاً موجوداً
3. **إضافة المنتجات**:
   - اختر منتجاً من القائمة
   - حدد الوحدة (يتم تحميلها تلقائياً)
   - أدخل الكمية (سيظهر المخزون المتاح)
   - عدّل السعر إن رغبت (يظهر سعر البيع افتراضياً)
   - أدخل خصم إن رغبت
   - اضغط "إضافة" لإضافة البند
4. **حفظ الفاتورة**: اضغط "حفظ الفاتورة" لحفظها كمسودة (سيتم توليد رقم تلقائي)
5. **ترحيل الفاتورة**: اضغط "ترحيل الفاتورة" لتحديث المخزون ورصيد العميل

### ميزات فاتورة البيع المحسّنة

- **رقم تلقائي**: يتم توليد رقم الفاتورة تلقائياً عند الحفظ
- **ضريبة مرنة**: تحميل نسبة الضريبة من إعدادات النظام (افتراضي 14%)
- **حسابات دقيقة**: خصومات على مستوى البند وضريبة على الإجمالي
- **تحقق المخزون**: منع بيع أكثر من المخزون المتاح
- **تحديث فوري**: إعادة حساب الإجماليات عند تغيير الكمية/السعر/الخصم
- **حالة انشغال**: تعطيل الأزرار أثناء الحفظ/الترحيل
- **معاملات آمنة**: حفظ وترحيل داخل معاملات قاعدة بيانات

### NullReference عند فتح نافذة فاتورة البيع

- **السبب**: ربط DataGrid بخصائص غير موجودة أو استدعاء حسابات قبل تهيئة UI
- **الحل المطبق**:
  - ربط أعمدة DataGrid بالخصائص الصحيحة: `Product.ProductName`, `Unit.UnitName`
  - إضافة فحوصات null في جميع طرق الحساب
  - فحص `IsLoaded` قبل استدعاء الحسابات في أحداث TextChanged
  - حماية try/catch في منشئ النافذة والطرق الحساسة

### لا تظهر الشاشة الرئيسية بعد تسجيل الدخول

- السبب: في WPF إذا فُتحت نافذة تسجيل الدخول أولاً بدون تعيين `MainWindow`، قد تعتبر WPF نافذة الدخول هي `MainWindow`. ومع وضع الإغلاق `OnMainWindowClose` سيُغلق التطبيق بمجرد إغلاق نافذة الدخول حتى لو تم التحقق بنجاح.
- الحل المطبق: جعل وضع الإغلاق `OnExplicitShutdown` أثناء عرض نافذة الدخول، ثم إعادته إلى `OnMainWindowClose` بعد إظهار الشاشة الرئيسية.
- تفاصيل التنفيذ: تم تعديل `App.xaml.cs` ليصبح التسلسل كالتالي:
  1) `this.ShutdownMode = ShutdownMode.OnExplicitShutdown;` قبل عرض نافذة الدخول.
  2) بعد نجاح الدخول: تعيين `MainWindow = new MainWindow(...)` ثم `this.ShutdownMode = ShutdownMode.OnMainWindowClose;` وأخيراً `MainWindow.Show()`.

إذا واجهت هذه المشكلة، تأكد من أن الكود يتبع هذا النمط.

### مشكلة اتصال قاعدة البيانات LocalDB

- إذا لم تكن SQL Server LocalDB مثبتة، ثبّت SQL Server Express LocalDB أو غيّر سلسلة الاتصال في `appsettings.json` إلى خادم SQL متاح.
- السلسلة الافتراضية: `Server=(localdb)\\mssqllocaldb;Database=AccountingSystemDB;Trusted_Connection=true;MultipleActiveResultSets=true`.

### بيانات المستخدم الافتراضية

- تم تهيئة مستخدم `admin` بكلمة مرور `Admin@123` تلقائياً عبر تهيئة قاعدة البيانات (EF Core Migrations + Seed).
- مستخدم `accountant` بكلمة مرور `Account@123` للمحاسب.
- إذا فشل الدخول: تحقق من وجود جدول `Users` والقيود، أو شغل ترحيلات EF يدويًا.

## الإعدادات

### قاعدة البيانات

يمكن تغيير سلسلة الاتصال في ملف `appsettings.json`:

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=(localdb)\\mssqllocaldb;Database=AccountingSystemDB;Trusted_Connection=true;MultipleActiveResultSets=true"
  }
}
```

## المستخدم الافتراضي

- **اسم المستخدم**: admin
- **كلمة المرور**: admin

## الخصائص المتقدمة

### 🔄 التكامل التلقائي

- تحديث المخزون فور ترحيل الفواتير
- حساب الأرصدة التراكمية تلقائياً
- منع العمليات غير الصحيحة محاسبياً

### 📊 التقارير التفاعلية

- تقارير قابلة للتخصيص
- تصدير للـ Excel و PDF
- مرشحات متقدمة للبيانات

### 🔧 قابلية التوسع

- هيكل طبقي قابل للصيانة
- استخدام أنماط التصميم المعيارية
- إمكانية إضافة ميزات جديدة بسهولة

## خطط التطوير المستقبلية

- [ ] تطبيق المحمول للاستعلام
- [ ] تكامل مع أنظمة ERP
- [ ] تقارير ذكية مع الذكاء الاصطناعي
- [ ] API للتكامل مع التطبيقات الأخرى

## المساهمة

نرحب بمساهماتكم! يرجى إنشاء Pull Request أو فتح Issue لأي اقتراحات أو مشاكل.

## الترخيص

هذا المشروع مرخص تحت [MIT License](LICENSE).

## الدعم

للحصول على الدعم، يرجى:

- فتح Issue في GitHub
- التواصل عبر البريد الإلكتروني
- مراجعة الوثائق

---

**تم التطوير بواسطة**: GitHub Copilot  
**الإصدار**: 1.1.0  
**تاريخ الإصدار**: سبتمبر 2025  
**آخر تحديث**: إصلاح فاتورة البيع ونظام الصلاحيات
